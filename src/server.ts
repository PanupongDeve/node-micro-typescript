
import * as express  from 'express';

const app: express.Express = express();

// plugin - middleware

// plugin - api


// app running

app.listen(3000, () => {
    console.log('server running port 3000')
})